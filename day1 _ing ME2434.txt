#include<stdio.h>
int main() {
int a,b;
printf("Enter two numbers:");
scanf("%d%d",&a,&b);
printf("add(a+b)=%d\n",a+b);
printf("sub(a-b)=%d\n",a-b);
printf("mul(a*b)=%d\n",a*b);
printf("div(a/b)=%d\n",a/b);
return 0;
}
#include<stdio.h> 
int main()
{
    int num =7;
    if(num==7) {
        printf("Number is positive\n");
    }
    else{
        printf("Number is negative\n");
    }
    return 0;
}